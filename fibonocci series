#include<stdio.h>
void fib(int n)
{
	int i,a=0,b=1,next=0;
	printf("%d %d ",a,b);
	for(i=2;i<n;i++)
	{
		next=a+b;
		printf("%d ",next);
		a=b;
		b=next;
	}
}




int main()
{
	int n;
scanf("%d",&n);
fib(n);
return 0;
}
